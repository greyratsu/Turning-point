name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Setup Java JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Install NPM Dependencies
        run: |
          npm init -y
          npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/assets

      - name: Bundle Web Assets into www Directory
        run: |
          mkdir -p www
          rsync -av --exclude='www' --exclude='node_modules' --exclude='.git' --exclude='.github' --exclude='android' --exclude='assets' ./ www/

      - name: Initialize Capacitor Configuration
        run: |
          npx cap init "LevelUp" "com.greyratsu.LevelUp" --web-dir www

      - name: Add Android Platform
        run: |
          npx cap add android

      # Corrected npx command name below
      - name: Generate App Icons
        run: |
          npx @capacitor/assets generate --android

      - name: Sync Capacitor Assets
        run: |
          npx cap copy android

      - name: Build Debug APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
